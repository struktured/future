language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-travisci-skeleton/master/.travis-opam.sh
script: bash -ex .travis-opam.sh

env:
  - OCAML_VERSION=latest PACKAGE=future INSTALL=false DEPOPTS=true TESTS=false

notifications: 
  email: 
    recipients: 
    - biocaml-commits@googlegroups.com
